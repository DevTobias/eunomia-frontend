const listStyle = {
  // backgroundColor: 'white',
  padding: 5,
  margin: 20,
  maxWidth: 400,
  width: 400,
};

const itemStyle = {
  borderLeft: '1px solid #292D33',
  borderRight: '1px solid #292D33',
  margin: '5px 0',
};

const beforeStyle = {
  backgroundColor: '#34C3FF',
  height: 5,
  margin: '5px 0',
  position: 'default',
};

const checkedBeforeStyle = {
  backgroundColor: '#292D33',
  height: 5,
  margin: '5px 0',
};

const styleCenter = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  maxHeight: '60px',
};

const iconStyle = {
  color: 'darkgrey',
  fontSize: '1.5em',
};

const baseStyle = {
  flexDirection: 'column',
  alignItems: 'flex-start',
  overflow: 'hidden',
};

const slimText = {
  fontSize: '0.666em',
  color: '#97969B',
  fontWeight: 'lighter',
  paddingBottom: 5,
};

const date = {
  paddingTop: 5,
};

const titleStyle = {
  paddingBottom: 5,
  whiteSpace: 'nowrap',
  fontWeight: 500,
  color: 'white',
};

const checkedTitleStyle = {
  paddingBottom: 5,
  whiteSpace: 'nowrap',
  fontWeight: 500,
  textDecoration: 'line-through',
  color: '#292D33',
};

const inputStyles = {
  marginBottom: 10,
};

export {
  listStyle, itemStyle, beforeStyle, checkedBeforeStyle, styleCenter, iconStyle, baseStyle,
  slimText, date, titleStyle, checkedTitleStyle, inputStyles,
};
